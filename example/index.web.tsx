import { AppRegistry } from 'react-native';
import App from './App';

AppRegistry.registerComponent('BitNetExample', () => App);
AppRegistry.runApplication('BitNetExample', {
  rootTag: document.getElementById('root'),
});
